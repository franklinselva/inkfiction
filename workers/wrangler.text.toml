# Wrangler configuration for AI Text Generation Worker
name = "inkfiction-ai-text"
main = "src/ai-text/index.ts"
compatibility_date = "2024-12-01"

# Development settings
[dev]
port = 8787
local_protocol = "http"

# Environment variables (use wrangler secret for production)
[vars]
# GEMINI_API_KEY is set via `wrangler secret put GEMINI_API_KEY`
